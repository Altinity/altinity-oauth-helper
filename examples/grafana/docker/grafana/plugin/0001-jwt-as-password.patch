diff --git a/pkg/client.go b/pkg/client.go
index f4e0848..9af4456 100644
--- a/pkg/client.go
+++ b/pkg/client.go
@@ -10,6 +10,7 @@ import (
 	"net/http"
 	"net/url"
 	"slices"
+	"strings"
 	"time"
 
 	"compress/flate"
@@ -26,6 +27,13 @@ var TimeZoneQuery = fmt.Sprintf("SELECT %s FORMAT JSON;", TimeZoneFieldName)
 
 type ClickHouseClient struct {
 	settings *DatasourceSettings
+	// OAuth pass-through (Altinity OSS demo patch).
+	// When Authz starts with "Bearer " and UserEmail is non-empty, Query()
+	// repacks them as HTTP Basic credentials "<UserEmail>:<jwt>" so that
+	// OSS ClickHouse (which doesn't understand Bearer) can authenticate
+	// the request via the ch-jwt-verify sidecar.
+	Authz     string
+	UserEmail string
 }
 
 func (client *ClickHouseClient) Query(ctx context.Context, query string) (*Response, error) {
@@ -61,7 +69,15 @@ func (client *ClickHouseClient) Query(ctx context.Context, query string) (*Respo
 		params.Add("enable_http_compression", "1")
 		req.URL.RawQuery = params.Encode()
 	}
-	if client.settings.Instance.BasicAuthEnabled {
+	if strings.HasPrefix(client.Authz, "Bearer ") && client.UserEmail != "" {
+		// OAuth pass-through: pack the user's JWT into HTTP Basic auth so
+		// OSS ClickHouse + ch-jwt-verify can authenticate the query as the
+		// logged-in Grafana user. Requires `oauthPassThru: true` on the
+		// datasource — without it, Authz is empty and we fall through to
+		// the static-creds path below.
+		jwt := strings.TrimPrefix(client.Authz, "Bearer ")
+		req.SetBasicAuth(client.UserEmail, jwt)
+	} else if client.settings.Instance.BasicAuthEnabled {
 		password := client.settings.Instance.DecryptedSecureJSONData["basicAuthPassword"]
 		req.SetBasicAuth(client.settings.Instance.BasicAuthUser, password)
 	} else if client.settings.UseYandexCloudAuthorization {
diff --git a/pkg/datasource.go b/pkg/datasource.go
index ad2e107..e45a901 100644
--- a/pkg/datasource.go
+++ b/pkg/datasource.go
@@ -28,7 +28,7 @@ func (ds *ClickHouseDatasource) getClient(ctx context.Context, pluginCtx backend
 	}, nil
 }
 
-func (ds *ClickHouseDatasource) executeQuery(pluginContext backend.PluginContext, ctx context.Context, query *Query) backend.DataResponse {
+func (ds *ClickHouseDatasource) executeQuery(pluginContext backend.PluginContext, ctx context.Context, query *Query, authz, userEmail string) backend.DataResponse {
 
 	onErr := func(err error) backend.DataResponse {
 		backend.Logger.Error(fmt.Sprintf("Datasource executeQuery error: %s", err))
@@ -39,6 +39,9 @@ func (ds *ClickHouseDatasource) executeQuery(pluginContext backend.PluginContext
 	if err != nil {
 		return onErr(err)
 	}
+	// OAuth pass-through threaded from QueryData (Altinity OSS demo patch).
+	client.Authz = authz
+	client.UserEmail = userEmail
 	sql := query.ApplyTimeRangeToQuery()
 	clickhouseResponse, err := client.Query(ctx, sql)
 	if err != nil {
@@ -56,7 +59,7 @@ func (ds *ClickHouseDatasource) executeQuery(pluginContext backend.PluginContext
 	}
 }
 
-func (ds *ClickHouseDatasource) evalQuery(pluginContext backend.PluginContext, ctx context.Context, evalQuery *eval.EvalQuery) backend.DataResponse {
+func (ds *ClickHouseDatasource) evalQuery(pluginContext backend.PluginContext, ctx context.Context, evalQuery *eval.EvalQuery, authz, userEmail string) backend.DataResponse {
 	onErr := func(err error) backend.DataResponse {
 		backend.Logger.Error(fmt.Sprintf("Datasource evalQuery error: %s", err))
 		return backend.DataResponse{Error: err}
@@ -72,7 +75,7 @@ func (ds *ClickHouseDatasource) evalQuery(pluginContext backend.PluginContext, c
 		To:       evalQuery.To,
 		RawQuery: sql,
 	}
-	return ds.executeQuery(pluginContext, ctx, &q)
+	return ds.executeQuery(pluginContext, ctx, &q, authz, userEmail)
 }
 
 func (ds *ClickHouseDatasource) QueryData(
@@ -87,6 +90,13 @@ func (ds *ClickHouseDatasource) QueryData(
 	var mu sync.Mutex
 	wg, wgCtx := errgroup.WithContext(ctx)
 	ruleUid := req.Headers["X-Rule-Uid"]
+	// OAuth pass-through (Altinity OSS demo patch). Pulled per-request so
+	// each query carries the logged-in user's token instead of a static one.
+	authz := req.GetHTTPHeader("Authorization")
+	var userEmail string
+	if req.PluginContext.User != nil {
+		userEmail = req.PluginContext.User.Email
+	}
 	for _, query := range req.Queries {
 		var evalQ = eval.EvalQuery{
 			RuleUid:       ruleUid,
@@ -97,7 +107,7 @@ func (ds *ClickHouseDatasource) QueryData(
 		evalJsonErr := json.Unmarshal(query.JSON, &evalQ)
 		if evalJsonErr == nil {
 			wg.Go(func() error {
-				result := ds.evalQuery(req.PluginContext, wgCtx, &evalQ)
+				result := ds.evalQuery(req.PluginContext, wgCtx, &evalQ, authz, userEmail)
 				mu.Lock()
 				response.Responses[evalQ.RefId] = result
 				mu.Unlock()
@@ -115,7 +125,7 @@ func (ds *ClickHouseDatasource) QueryData(
 				return onErr(fmt.Errorf("unable to parse json, to Query error: %v, to EvalQuery error: %v, source JSON: %s", jsonErr, evalJsonErr, query.JSON))
 			}
 			wg.Go(func() error {
-				result := ds.executeQuery(req.PluginContext, wgCtx, &q)
+				result := ds.executeQuery(req.PluginContext, wgCtx, &q, authz, userEmail)
 				mu.Lock()
 				response.Responses[q.RefId] = result
 				mu.Unlock()
